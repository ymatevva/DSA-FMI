#include<iostream>

//Calculate the complexity:

//Example 1:
int average(int num1, int num2){
    return (num1+num2)/2;
}
//The complexity is O(1), bc for every input the algorithm makes one calculation(one step)

